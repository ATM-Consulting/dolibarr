- FIX : Durcissement de l'API REST de reinitialisation - aucun lien n'est envoye pour un compte qui n'a pas le droit `user > self > password` (meme regle que la page web), ni pour une adresse email partagee par plusieurs comptes (impossible de designer lequel), et une nouvelle demande est bridee pendant `USER_PASSWORD_RESET_MIN_INTERVAL` secondes (60 par defaut, 0 desactive) faute de captcha sur l'API. Dans les trois cas la reponse reste `{success:true}` (anti-enumeration) et rien n'est arme. - *2026-09-09*
- NEW : API REST de reinitialisation alignee sur le nouveau systeme du coeur - jeton expirant au lieu d'un mot de passe genere stocke en clair dans pass_temp, hash force en type 'hash' (deterministe et url-safe quel que soit MAIN_SECURITY_HASH_ALGO), lien expire distingue du lien invalide (410 au lieu de 403), corps du mail partage avec la page web, et nouvel endpoint `passwordresetvalidate` pour que le portail n'affiche son formulaire que si le lien est encore vivant - depend du backport `SPECIFIQUE ATM password-reset-native` - *2026-09-09*
- NEW : Password reset by expiring link - the user chooses their own password, no cleartext password is ever emailed (forgotten-password page, admin "send new password" button, REST users/{id}/setPassword) - backport of core PR #39370, native in v25 - branch NEW/PasswordResetLinkPR39370 - *2026-09-09*
  - REMOVAL ON THE v25 UPGRADE (v25 ships this natively, so this backport must be dropped, not merged). `git grep 'SPECIFIQUE ATM password-reset-native'` lists every piece. v25 equivalents: atmGetPasswordResetHash/atmVerifyPasswordResetHash -> dolGetPasswordResetHash/dolVerifyPasswordResetHash (core/lib/security2.lib.php), atmRequestPasswordReset -> User::requestPasswordReset(), atmGetPasswordResetEmailContent -> User::getPasswordResetEmailContent(), atmSendPasswordResetLink -> the rewritten body of User::send_password(), atmVerifyCaptchaCode -> inlined in passwordforgotten.php.
    - `git rm` whole (100% ATM): htdocs/core/lib/atm_passwordreset.lib.php, htdocs/core/tpl/atm_passwordreset.tpl.php, test/phpunit/AtmPasswordResetTest.php
    - htdocs/user/passwordforgotten.php - 6 blocks: restore `$passworduidhash = GETPOST('passworduidhash', 'alpha');` and drop the $newpass1/$newpass2 reads plus the lib require; drop the early $disabled/$captcha block placed before the Actions section; drop the whole `action == 'setnewpassword'` step; restore the two `$sessionkey` / `$ok` captcha lines; restore `$newpassword = $edituser->setPassword($user, '', 1);`; restore `include $template_dir.'passwordreset.tpl.php';`
    - htdocs/user/card.php - 2 blocks: drop the ATM `confirm_passwordsend` handler, and restore the original two-branch condition of the "Change password with a new generated one" block (kept in comment right above it)
    - htdocs/user/class/user.class.php - 1 block: drop the 4 delegating lines at the top of send_password(); the untouched core body below takes over again
    - htdocs/user/class/api_users.class.php - 1 block + 1 PHPDoc mention: drop the `if ($send_password)` short-circuit at the top of setPassword(), and restore the original `@param bool $send_password` / `@return` wording
    - htdocs/langs/en_US/users.lang and htdocs/langs/fr_FR/users.lang - drop the 4 keys between the `# DEBUT` / `# FIN SPECIFIQUE ATM` comment markers (PasswordResetLinkSentTo, NewPasswordMismatch, NewPasswordEmpty, PasswordResetLinkExpired); v25 ships them in en_US, Transifex brings fr_FR
    - htdocs/core/tpl/passwordreset.tpl.php - NOT modified by this backport, nothing to restore
    - Data: pass_temp holds 'r:<YYYYMMDDHHMMSS gmt>:<secret>' - no schema change (varchar(128) already), and v25 uses the very same format, so pending links stay valid across the upgrade
- FIX : DA028671 - Changing the third party of a ticket now moves its agenda events too - Ticket::setCustomer() updated only llx_ticket, so the events already recorded stayed on the previous customer and never appeared on the new one (the third party Agenda tab filters on actioncomm.fk_soc alone) - *2026-09-03*
- NEW : Configuration UI (Accueil > Configuration > Sécurité, onglet Mots de passe) pour l'API de réinitialisation de mot de passe : activation de `USER_PASSWORD_RESET_PUBLIC_API` + champ `USER_PASSWORD_RESET_ALLOWED_RETURN_HOSTS` (plus besoin de passer par les constantes manuelles). - *2026-08-03*
- FIX : Doc preview (magnifier/loupe) on shipments (BL) in thirdparty card (client/prospect tab) - use 'expedition' modulepart instead of $sendingstatic->element ('shipping') in comm/card.php - PR #755 - *2026-07-09*
- NEW : API REST réinitialisation de mot de passe (`passwordpolicy` / `passwordresetrequest` / `passwordresetconfirm` dans api_users) pour un flux "mot de passe oublié" transparent côté portail ; opt-in `USER_PASSWORD_RESET_PUBLIC_API`, lien email vers `returnurl` whitelisté (`USER_PASSWORD_RESET_ALLOWED_RETURN_HOSTS`), anti-énumération, validation politique via setPassword. - *2026-06-24*
- FIX : BACKPORT pour SENTRY : add null check before setting _renderItem on jQuery UI autocomplete widget #37597
- NEW : Add complete_substitutions_array in ticket.class.php - *2026-02-20*
- FIX : DA027856 - Edition des extrafields "always editable" sur les projets avec une tache ouverte en bas de page.
- FIX : DA027490 - BACKPORT 37132 - Date sur les temps passés sur les projets était erronée. 
- FIX : DA027053 - Update tms on delete propalLine if propalLine is a product with a price of 0 or if product is not include.
- FIX : DA026698 - Backport from v23 - Recherche insensible aux diacritiques (accents)
- FIX : DA026536 - Saut de ligne dans mail automatique - A supp en 21.0.3
- FIX : DA026716 - Doc_ref too long
- FIX : DA026571 - L'approbation de congé via la massaction n'enregistre pas la date d'approbation
- FIX : #DA026469 -  Correction d’un problème où le bouton "Joindre un fichier" lors de la création d’un ticket déclenchait la création du ticket au lieu de permettre l’ajout de fichiers.
- FIX: #DA026519 - input hidden did not work properly on form mail of ticket module
- FIX create/edit ticket forms
- Backport from 20.0 :
  - Added hook into getElementProperties to handle $element_properties array
- Backport from 17.0_atm : 
    - Add soc TVA on clone propal (dev to integrate in 20.0)
    - Hide external user in combobox
    - FIX hide non employee user
    - Mention "B2B" for direct debit voucher
