- Backported from develop PR #33800 : Supplier proposals delays
- OREP specific, submitted upstream as PR #40155 (https://github.com/Dolibarr/dolibarr/pull/40155) : refuse a duplicate customer order reference (`ref_client`) on the same third party, mirroring the native `uk_facture_fourn_ref_supplier (ref_supplier, fk_soc, entity)` unique index of supplier invoices. Enforced on creation, on inline edition of the customer reference and on the REST API update, with an error naming the conflicting order. No unique index is added: the existing data holds duplicates. Code marked in place with `/** UPSTREAM PR #40155 */` / `/** END UPSTREAM PR #40155 */`, so `grep -rn "UPSTREAM PR #40155"` lists every touched spot on a major upgrade. Aligned on the upstream review feedback (upstream commits f1843c6 and 0a43e0e): method named `isRefClientAlreadyUsed()`, returning 1 when the ref is already used, 0 when free, the negative range being reserved for technical errors, and opt-out through `ORDER_ALLOW_DUPLICATE_REF_CLIENT` (hidden, the PR setup toggle was dropped by the maintainer). **Two additions stay OREP specific**: the exemption list `ORDER_REF_CLIENT_DUPLICATE_ALLOWED_VALUES` (comma separated, case and spacing insensitive, empty by default) which the PR does not carry and which this instance needs for the placeholder references repeated on purpose, and `getOrderUsingSameRefClient()` kept split out where the PR inlines the query. If the target version ships the PR: drop `getOrderUsingSameRefClient()`, `isRefClientExemptFromDuplicateControl()` and the body of `isRefClientAlreadyUsed()`, keep the call sites untouched, and re-implement the exemption list only if placeholder references are still in use. Files: EDIT `htdocs/commande/class/commande.class.php` (NEW `getOrderUsingSameRefClient()` + `isRefClientExemptFromDuplicateControl()` + `isRefClientAlreadyUsed()`, called from `create()`, `set_ref_client()` and `update()`) + `htdocs/langs/{en_US,fr_FR}/orders.lang`, NEW `test/phpunit/CommandeRefClientDuplicateTest.php`.
- Backported from 23.0 PR #37329 (Exchange-only subset) : Microsoft Exchange Online OAuth2 — new provider `Microsoft3` with strict scopes `offline_access,https://outlook.office.com/SMTP.Send,https://outlook.office.com/IMAP.AccessAsUser.All` (avoids AADSTS28000 from mixing Graph + Exchange resources). Files: NEW `htdocs/includes/OAuth/OAuth2/Service/Microsoft3.php`, NEW `htdocs/core/modules/oauth/microsoft3_oauthcallback.php`, EDIT `htdocs/core/lib/oauth.lib.php` + `htdocs/admin/oauth.php` + `htdocs/langs/{en_US,fr_FR}/oauth.lang`.
