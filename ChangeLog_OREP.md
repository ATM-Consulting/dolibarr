- Backported from develop PR #33800 : Supplier proposals delays
- OREP specific : refuse a duplicate customer order reference (`ref_client`) on the same third party, mirroring the native `uk_facture_fourn_ref_supplier (ref_supplier, fk_soc, entity)` unique index of supplier invoices. Enforced on creation, on inline edition of the customer reference and on the REST API update, with an error naming the conflicting order. No unique index is added: the existing data holds duplicates. Placeholder customer refs meant to be repeated (`BPA DEVIS` and the like) must be listed in the hidden option `ORDER_REF_CLIENT_DUPLICATE_ALLOWED_VALUES` (comma separated, case and spacing insensitive, empty by default). Files: EDIT `htdocs/commande/class/commande.class.php` (NEW `getOrderUsingSameRefClient()` + `isRefClientExemptFromDuplicateControl()` + `checkRefClientIsFree()`, called from `create()`, `set_ref_client()` and `update()`) + `htdocs/langs/{en_US,fr_FR}/orders.lang`, NEW `test/phpunit/CommandeRefClientDuplicateTest.php`.
- Backported from 23.0 PR #37329 (Exchange-only subset) : Microsoft Exchange Online OAuth2 — new provider `Microsoft3` with strict scopes `offline_access,https://outlook.office.com/SMTP.Send,https://outlook.office.com/IMAP.AccessAsUser.All` (avoids AADSTS28000 from mixing Graph + Exchange resources). Files: NEW `htdocs/includes/OAuth/OAuth2/Service/Microsoft3.php`, NEW `htdocs/core/modules/oauth/microsoft3_oauthcallback.php`, EDIT `htdocs/core/lib/oauth.lib.php` + `htdocs/admin/oauth.php` + `htdocs/langs/{en_US,fr_FR}/oauth.lang`.
